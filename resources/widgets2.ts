export const HelloWorld = async (event: any = {}) : Promise <any> => {
    // // TODO: https://webpack.js.org/
    // const svgString = toSvg("Suella", 300)

    return { statusCode: 200, body: "Hello from TS" };
};
